server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        resolver 8.8.8.8 valid=30s;
        proxy_pass https://rooty.onrender.com/api/;
        proxy_http_version 1.1;
        proxy_set_header Host rooty.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Evitar caché en el Service Worker y el manifiesto
    location ~* \.(js|json|webmanifest)$ {
        expires off;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Caché solo para assets dentro de la carpeta /assets/
    location ~* /assets/.*\.(js|css|png|jpg|svg|ico|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
